 27. they still have the same pattern
 28.it reset method to the default value ( they reset internal Arrays
 31.Using nextState avoids interference between updates. If you update state[i] directly, subsequent calculations for other cells might use already-updated values, leading to incorrect results.
33. i created a new method called update with the code